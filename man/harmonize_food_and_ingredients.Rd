% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize_food_and_ingredients.R
\name{harmonize_food_and_ingredients}
\alias{harmonize_food_and_ingredients}
\title{Harmonize Food Items and Ingredients}
\usage{
harmonize_food_and_ingredients(
  food_details,
  food_ingredients,
  key = "survey_id"
)
}
\arguments{
\item{food_details}{A data frame with food-level information (must include
the column specified in \code{key} and \code{desc_of_food}).}

\item{food_ingredients}{A data frame with ingredient-level information (must include
the column specified in \code{key} and \code{food_ingredients_used}).}

\item{key}{Character string. The column name that uniquely links
records across \code{food_details} and \code{food_ingredients}.
Defaults to \code{"survey_id"}.}
}
\value{
A tibble with two columns:
\describe{
\item{\if{html}{\out{<key>}}}{Survey identifier (column name matches \code{key} argument)}
\item{food_item}{Name of food or ingredient}
}
}
\description{
Produces a unified dataset of foods and ingredients reported in a dietary recall,
keeping only the survey identifier (\code{key}) and the harmonized food item name.
}
\details{
\itemize{
\item Foods with \code{NA} in \code{desc_of_food} are excluded.
\item No deduplication is forced: if the same item appears in both
foods and ingredients, both records are returned.
\item This function is primarily used for harmonizing raw item names before
linking to external food composition or coding systems.
}
}
\examples{
data("dietrecall_example")
result <- harmonize_food_and_ingredients(
  food_details = dietrecall_example$food_details,
  food_ingredients = dietrecall_example$food_ingredients_group,
  key = "survey_id"
)
head(result)

}
